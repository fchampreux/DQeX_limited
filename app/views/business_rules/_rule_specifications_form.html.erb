          <div class="row mat-form-row">
            <div class="mat-form-field col-md-3">
              <%= f.label :ordering_sequence, t('Sequence'), class: "mat-form-field-label" %>
              <%= f.text_field :ordering_sequence, class: "mat-input-element" %>
            </div>
            <div class="mat-form-field col-md-3">
              <%= f.label :is_published, class: "mat-form-field-label mat-checkbox-label" do %>
                <%= t('IsPublished') %>
                <%= f.check_box :is_published, class: "mat-checkbox-input" %>
              <% end %>
            </div>
          </div>
          <div class="row" >
            <div class="col-md-6">
              <%= render partial: "shared/translated_field_form", locals: {
                f: f,
                field: "business_value",
                fields: :business_value_translations,
                translations: @business_rule.business_value_translations,
                isTextarea: true,
                label: t('BusinessValue') } %>
            </div>
            <div class="col-md-6">
              <div class="row mat-form-row">
                <div class="mat-form-field col-md-6">
                  <%= f.label :rule_type_id, t('RuleType'), class: "mat-form-field-label" %>
                  <%= f.collection_select :rule_type_id, options_for('rules_types'), :id, :name, {}, { class: "mat-input-element select2-candidate" } %>
                </div>
              </div>
              <div class="row mat-form-row">
                <div class="mat-form-field col-md-6">
                  <%= f.label :rule_class_id, t('RuleClass'), class: "mat-form-field-label" %>
                  <%= f.collection_select :rule_class_id, options_for('rules_classes'), :id, :name, {}, { class: "mat-input-element select2-candidate" } %>
                </div>
              </div>
              <div class="row mat-form-row">
                <div class="mat-form-field col-md-6">
                  <%= f.label :severity_id, t('Severity'), class: "mat-form-field-label" %>
                  <%= f.collection_select :severity_id, options_for('rules_severity'), :id, :name, {}, { class: "mat-input-element" } %>
                </div>
              </div>
            </div>
          </div>
