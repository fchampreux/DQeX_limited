$("#skill-workflow").html(
  "<%= j (render partial: "shared/workflow_buttons", locals: {this_object: @skill}) %>"
);
$(document).trigger('proto.notification', { type: "<%= j (@alert_type) %>", message: "<%= j (@alert_notice) %>" });
