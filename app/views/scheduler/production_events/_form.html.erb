<%= form_with(model: production_event, local: true) do |form| %>
  <% if production_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(production_event.errors.count, "error") %> prohibited this production_event from being saved:</h2>

      <ul>
      <% production_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
