<%= form_with(model: production_job, local: true) do |form| %>
  <% if production_job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(production_job.errors.count, "error") %> prohibited this production_job from being saved:</h2>

      <ul>
      <% production_job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
